---
export interface Props {
  url: string;
}

const { url } = Astro.props;
---

<div
  class="fixed bottom-4 right-4 bg-white bg-opacity-80 p-2 rounded shadow text-sm text-gray-600"
>
  Made with <span class="text-red-500">&hearts;</span> in Prague, <a
    href={url}
    class="underline">open-sourced</a
  >
  to the world.
</div>
